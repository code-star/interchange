apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: backend
spec:
  rules:
    - host: "f35bc8ecd461e4a5ab1c2ce4cdefe762.sk1.eu-central-1.eks.amazonaws.com"
      http:
        paths:
          - backend:
              serviceName: codestar-interchange-backend-service
              servicePort: 8080